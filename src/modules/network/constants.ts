'use strict';

/** constants */
const JSON_CONTENT_TYPE: string = 'application/json;charset=utf-8';
const CONTENT_TYPE_REGEXP: RegExp = /[^/]+\/([^;]+);/;

/** export */
export {
  JSON_CONTENT_TYPE,
  CONTENT_TYPE_REGEXP,
}